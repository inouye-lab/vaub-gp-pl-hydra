_target_: src.data.mnist_usps_pair_enc_computed_datamodule.MNISTUSPSPairEncModule
data_dir: ${paths.data_dir}
batch_size: 1024 # Needs to be divisible by the number of devices (e.g., if in a distributed setup)
num_workers: 16
pin_memory: False